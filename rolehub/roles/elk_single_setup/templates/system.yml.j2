- module: system
  syslog:
    enabled: true
    var.paths: ["/var/log/syslog*", "/var/log/messages*"] # remember we can use iteration here to use multiple log files
  auth:
    enabled: true
    var.paths: ["/var/log/auth.log*", "/var/log/secure*"]
